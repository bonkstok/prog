#modules
#~ import calendar
#~ from time import time,ctime
#~ year = int(input("Enter a year:"))
#~ calendar.prcal(year)
#~ prev_time=""
#~ print("Ctime:\t",ctime())
#~ print("Time\t",time())
#~ print("Comb\t", ctime(time()))
#~ while(True):
	#~ curr_time = ctime(time())
	#~ if(prev_time != curr_time):
		#~ print("The time is:", ctime(time()))
		#~ prev_time = curr_time
import sys,calendar
print(dir())
print(dir(calendar), '\n')
print(dir(sys))
